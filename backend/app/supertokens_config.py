# This file has been removed as part of the SuperTokens → OIDC migration.
# See app/oidc_config.py for the replacement.
raise ImportError("supertokens_config has been removed — use oidc_config instead")
