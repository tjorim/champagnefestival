<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug-only: permits cleartext HTTP to the local backend (10.0.2.2 is the
         emulator's alias for the host loopback) and to localhost, so debug builds
         can reach a locally-run backend/OIDC provider without TLS. Release builds
         use src/main and are unaffected. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
