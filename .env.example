# Public environment variables (accessible via import.meta.env.VITE_*)
VITE_PUBLIC_URL=
VITE_SITE_TITLE=Champagne Festival
VITE_API_BASE_URL=

# Mode (development, production, or custom)
# NODE_ENV=production

# Build variables
# These affect how the site is built and deployed
BUILD_PATH=dist